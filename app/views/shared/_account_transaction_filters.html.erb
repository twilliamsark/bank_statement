<%= form_with url: form_url,
      method: :get,
      local: true,
      data: {
        controller: "live-filter",
        live_filter_delay_value: 250,
        turbo_frame: "transactions"
      },
      class: "card-pad space-y-4" do %>
  <div class="flex flex-wrap items-end justify-between gap-2">
    <div>
      <p class="stat-label">Filters</p>
      <p class="mt-1 text-sm text-muted">Date and section apply immediately; description and amount update as you type.</p>
    </div>
    <% if filtered %>
      <%= link_to "Clear filters", clear_url,
            data: { turbo_frame: "transactions" },
            class: "text-sm font-semibold text-brand-700 hover:text-brand-800" %>
    <% end %>
  </div>

  <div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
    <div>
      <label for="date_from" class="block text-xs font-semibold uppercase tracking-wide text-slate-500">From</label>
      <%= date_field_tag :date_from, params[:date_from],
            class: "mt-1 w-full rounded-lg border border-line px-3 py-2 text-sm",
            data: { action: "change->live-filter#submitNow" } %>
    </div>
    <div>
      <label for="date_to" class="block text-xs font-semibold uppercase tracking-wide text-slate-500">To</label>
      <%= date_field_tag :date_to, params[:date_to],
            class: "mt-1 w-full rounded-lg border border-line px-3 py-2 text-sm",
            data: { action: "change->live-filter#submitNow" } %>
    </div>
    <div>
      <label for="section" class="block text-xs font-semibold uppercase tracking-wide text-slate-500">Section</label>
      <%= select_tag :section,
            options_for_select([["All sections", ""]] + sections.map { |s| [s, s] }, params[:section]),
            class: "mt-1 w-full rounded-lg border border-line px-3 py-2 text-sm",
            data: { action: "change->live-filter#submitNow" } %>
    </div>
    <div class="sm:col-span-2">
      <label for="description" class="block text-xs font-semibold uppercase tracking-wide text-slate-500">Description</label>
      <%= text_field_tag :description, params[:description],
            placeholder: "Partial match…",
            autocomplete: "off",
            class: "mt-1 w-full rounded-lg border border-line px-3 py-2 text-sm",
            data: { action: "input->live-filter#queue" } %>
    </div>
    <div>
      <label for="amount" class="block text-xs font-semibold uppercase tracking-wide text-slate-500">Amount</label>
      <%= text_field_tag :amount, params[:amount],
            placeholder: "e.g. 12.3 or -25",
            autocomplete: "off",
            class: "mt-1 w-full rounded-lg border border-line px-3 py-2 text-sm",
            data: { action: "input->live-filter#queue" } %>
    </div>
  </div>

  <noscript>
    <%= submit_tag "Apply filters", class: "btn-primary cursor-pointer" %>
  </noscript>
<% end %>
